<UserControl x:Class="Apv.MemberExcel.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:Apv.MemberExcel.Views"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance views:MainViewModel}">
    
    <StackPanel>
        <HeaderedContentControl Header="Adressen-Excel" Margin="12">
            <TextBox Text="{Binding ExcelFilePath}" Height="24" Padding="0 2 0 0"/>
        </HeaderedContentControl>
        <HeaderedContentControl Header="PDF-Ausgabe" Margin="12">
            <TextBox Text="{Binding PdfFolderPath}" Height="24" Padding="0 2 0 0"/>
        </HeaderedContentControl>
        <Button Content="PDFs erzeugen" Command="{Binding CreatePdfsCommand}" Margin="12" Height="24" MinWidth="128" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </StackPanel>
    
</UserControl>
